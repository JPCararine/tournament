spring.application.name=project

# ---------------------------------------------------------------------------
# Banco de dados (PostgreSQL gerenciado - Supabase/Neon)
# ---------------------------------------------------------------------------
spring.datasource.url=${DATABASE_URL}
spring.datasource.username=${DATABASE_USERNAME:}
spring.datasource.password=${DATABASE_PASSWORD:}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.open-in-view=false

# ---------------------------------------------------------------------------
# AbacatePay (gateway Pix)
# ---------------------------------------------------------------------------
abacatepay.api-url=${ABACATEPAY_API_URL:https://api.abacatepay.com}
abacatepay.token=${ABACATEPAY_TOKEN}
abacatepay.webhook-secret=${ABACATEPAY_WEBHOOK_SECRET}
# Valor da taxa de inscrição em centavos (ex.: 5000 = R$ 50,00)
abacatepay.amount-cents=${ABACATEPAY_AMOUNT_CENTS:5000}

# ---------------------------------------------------------------------------
# SendGrid (e-mail transacional)
# ---------------------------------------------------------------------------
sendgrid.api-key=${SENDGRID_API_KEY}
sendgrid.from-email=${SENDGRID_FROM_EMAIL}

# ---------------------------------------------------------------------------
# Aplicação
# ---------------------------------------------------------------------------
app.discord-invite-url=${DISCORD_INVITE_URL}
app.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:5173}
